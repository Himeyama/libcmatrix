#include <iostream>
#include <cmath>
#include "rational.cpp"
#include "matrix.cpp"

int main(){
    Rational c = 100.1_r;
    c.p();
    
    // std::cout << 1.0 << std::endl;


    //  (1.2 - (int)1.2 / 1)

    // Matrix <double> m(3);
    // m = {{1, 1, -1}, {-2, 0, -1}, {0, 2, 1}};
    // m.about();

    // m.p();
    // m.u().p();
    // m.l().p();
    // (m.l() * m.u()).p();
}